# 压力测试与调参 —— 零基础学习指南（第十一篇）

> **写给谁看：** 已完成 [第十篇](./10-MySQL登录注册指南.md)（`Step1to6/step10.cpp`）的学习者——功能已齐（静态站 + 状态机 + 定时器 + 分次写 + 日志 + MySQL 登录注册）。  
> **本篇目标：** 用 **Webbench** 给服务器「打分」，记录 **QPS / 成功率**，对比 **线程数、开/关日志、定时器** 等参数，学会用数据指导优化。  
> **不做的事：** 不追求复现 Tiny  README 里「九万 QPS」；本篇是**本机学习压测**，结果因 CPU、WSL、是否开 MySQL 而异。  
> **代码形式：** 在 **`step10.cpp` 基础上小改**，保存为 **`Step1to6/step11.cpp`**；本篇**不重贴 step10 全文**（见第五节「增量改动」）。  
> **前置文档：** [10-MySQL登录注册指南](./10-MySQL登录注册指南.md) · [Step5 之后路线图](./Step5之后完善路线图.md)

---

## 目录

1. [第一部分：Step11 是不是最后一步？](#第一部分step11-是不是最后一步)
2. [第二部分：压测在测什么](#第二部分压测在测什么)
3. [第三部分：安装 Webbench 与系统准备](#第三部分安装-webbench-与系统准备)
4. [第四部分：压测流程（手把手）](#第四部分压测流程手把手)
5. [第五部分：step11 代码增量改动](#第五部分step11-代码增量改动)（含 [5.7 完整代码](#57-完整代码)）
6. [第六部分：对比实验怎么设计](#第六部分对比实验怎么设计)
7. [第七部分：如何读懂 Webbench 输出](#第七部分如何读懂-webbench-输出)
8. [编译、运行与测试](#编译运行与测试)
9. [常见问题排查](#常见问题排查)
10. [本篇小结与下一篇预告](#本篇小结与下一篇预告)

---

## 第一部分：Step11 是不是最后一步？

### 1.1 直接回答

| 说法 | 对不对 |
|------|--------|
| **Step11 是 TinyWebServer 主路线的最后一步** | ✅ 对 |
| **Step11 之后 MyWebServer 再也没有任何步骤** | ❌ 不对 |
| **做完 Step11 就可以写简历「复现 Tiny 核心模块」** | ✅ 基本对（静态 + 并发 + 日志 + MySQL + 压测） |

路线图里 **Step6～Step11** 对齐 Tiny 的「功能 + 压测」；**Step12 是可选进阶**（ET、EPOLLONESHOT、Keep-Alive）。全部步骤完成后，你还可以：

- **拆分多文件**（你之前定的：等所有 step 写完再拆）
- **对照 Tiny 精修**（定时器链表、四缓冲日志等）
- **走 Step13+**（linyacool 的 `Webserver/` 多 Reactor 架构，另一条路线）

```text
Step5   线程池 + epoll + GET 静态
  …
Step10  MySQL + 注册登录
  ▼
Step11  Webbench 压测          ← 本篇（主路线收官）
  ▼
Step12（可选）ET / ONESHOT / Keep-Alive
  ▼
（可选）拆文件、对齐 Tiny 细节、多 Reactor 进阶
```

### 1.2 Step11 相对 Step10 升级了什么

| 项目 | Step10 | Step11 |
|------|--------|--------|
| 业务能力 | 注册 / 登录 | **不变** |
| 可配置性 | 常量写死在 cpp | **命令行** `-p -t -c` 等 |
| 压测友好 | 每条请求打日志 | 可 **`-c 1` 关日志** |
| 验收方式 | 浏览器点表单 | **webbench 数字** + 浏览器抽检 |

### 1.3 对照 TinyWebServer

| 本篇 | Tiny |
|------|------|
| `webbench -c -t URL` | `test_pressure/webbench-1.5/` |
| `./server -t -c` | `main` 里 `getopt` 解析线程数、关日志 |
| 压测前关日志 | README 强调 **close_log** 再测 |

---

## 第二部分：压测在测什么

### 2.1 什么叫 QPS

**QPS**（Queries Per Second）= 每秒完成的 HTTP 请求数。Webbench 跑完后会打印类似：

```text
Speed=12345 pages/min
123.45 requests/sec
```

第二行就是 **QPS**（约等于 `pages/min ÷ 60`）。

### 2.2 压什么 URL？为什么不压 `/login`

| URL | 是否适合主压测 | 原因 |
|-----|----------------|------|
| `http://127.0.0.1:8080/index.html` | ✅ **首选** | 纯静态 GET，测 epoll + 线程池 + 分次写 |
| `http://127.0.0.1:8080/about.html` | ✅ 可以 | 同上 |
| `http://127.0.0.1:8080/login` POST | ❌ 不作为主指标 | MySQL、表单解析、302 会拉低 QPS，且 webbench 默认是 **GET** |

**结论：** Step11 主实验用 **GET 静态页**；注册登录用 **浏览器 / curl 抽检** 即可（Step10 已验收）。

### 2.3 压测时服务器在干什么

```text
webbench  fork 出 N 个子进程
    每个子进程不断向 127.0.0.1:8080 发 GET /index.html
         │
         ▼
你的 server：accept → epoll → read → 状态机 → 线程池读盘拼响应
         → eventfd 唤醒 → 主线程 EPOLLOUT 写完 → close
```

瓶颈可能在：**线程池大小**、**日志写盘**、**fd 上限**、**listen backlog** 等——所以要 **改一个变量、记一组数**。

### 2.4 合理预期（别焦虑）

| 环境 | 大致 QPS 量级（关日志、静态页） |
|------|--------------------------------|
| 笔记本 WSL2 | 几千～一两万（差异很大） |
| Tiny README 宣传值 | 万级～九万（LT/ET + Proactor + 关日志 + 专门调优） |
| 学习项目 | **趋势对就行**：关日志 > 开日志；线程 4→8 可能升也可能因锁竞争略降 |

压测的意义是 **对比**，不是和一个 magic number 较劲。

---

## 第三部分：安装 Webbench 与系统准备

### 3.1 环境要求

- **Linux / WSL2**（与 Step4～10 一致；Windows 原生不便跑 webbench + epoll）
- 服务器已能正常：`curl http://127.0.0.1:8080/`

### 3.2 安装 Webbench（任选一种）

#### 方式 A：从 TinyWebServer 复制到 MyWebServer（推荐，与本仓库一致）

把 TinyWebServer 里的 **`test_pressure`** 整个文件夹复制到 **MyWebServer 项目根目录**（与 `www/`、`step11.cpp` 同级）即可：

```text
MyWebServer/
├── www/
├── step11.cpp
├── server              ← 编译出的可执行文件
└── test_pressure/      ← 从 Tiny 复制过来
    ├── README.md
    └── webbench-1.5/
        ├── Makefile
        ├── webbench.c
        └── ...
```

**可以这样做吗？** 可以。`webbench` 是独立的 C 压测工具，自带 `Makefile`，**不依赖** Tiny 的 `server` 源码；复制后在本仓库里编译、运行即可。

在 **WSL / Linux** 下编译（首次或更新源码后执行一次）：

```bash
cd /path/to/MyWebServer/test_pressure/webbench-1.5
make clean && make

# 应生成当前目录下的 ./webbench
./webbench --help
```

压测时用**相对项目根目录**的路径（另开终端，先启动 `./server`）：

```bash
# 在 MyWebServer 根目录
./test_pressure/webbench-1.5/webbench -c 100 -t 5 http://127.0.0.1:8080/index.html
```

> 更完整的 MyWebServer 压测说明见 [test_pressure/README.md](../test_pressure/README.md)。

#### 方式 B：留在 TinyWebServer 目录里编译（不复制时）

```bash
# 若你已 clone 过 TinyWebServer，且未复制到 MyWebServer
cd /path/to/TinyWebServer/test_pressure/webbench-1.5
make clean && make

# 当前目录会生成 ./webbench
./webbench --help
```

#### 方式 C：独立仓库

```bash
git clone https://github.com/EZLippi/WebBench.git
cd WebBench
make
./webbench --help
```

> 若 `make` 报 `ctags` 相关警告，一般可忽略；只要生成 `webbench` 可执行文件即可。部分环境需要：`sudo apt install build-essential`。

### 3.3 系统限制（压测前建议检查）

#### 文件描述符 `ulimit -n`

每个并发连接占一个 `fd`；webbench 的 `-c` 很大时，**服务器进程**和**系统**都要够。

```bash
ulimit -n          # 查看当前限制，常见 1024
ulimit -n 65535    # 当前 shell 临时调高（压测前在启动 server 的终端执行）
```

若 `-c 5000` 时 server 报 `Too many open files` 或 accept 失败，先调 `ulimit` 再测。

#### 端口与防火墙

本机 `127.0.0.1:8080` 一般无防火墙问题；若改端口，server 与 webbench URL 要一致。

#### MySQL

压静态页时 **MySQL 几乎不参与**；但 step11 仍会在启动时 `SqlPool::init`（与 step10 相同）。确保 MySQL 在跑，否则 server 起不来。

---

## 第四部分：压测流程（手把手）

### 4.1 标准四步

```text
1. 编译 step11，关日志、选线程数启动 server
2. 另开终端，确认 curl 正常
3. 运行 webbench（从小 -c 逐步加大）
4. 记录 QPS、失败数；改参数重复 2～3
```

### 4.2 终端 1：启动服务器（压测推荐参数）

```bash
cd /path/to/MyWebServer

g++ -std=c++17 -Wall -pthread -o server Step1to6/step11.cpp -lmysqlclient

# 关日志、4 线程、8080 端口（与 Tiny 习惯一致：压测前关日志）
ulimit -n 65535
./server -p 8080 -t 4 -c 1
```

参数含义见 [第五节](#第五部分step11-代码增量改动)。

### 4.3 终端 2：冒烟测试

```bash
curl -I http://127.0.0.1:8080/index.html
# 期望：HTTP/1.1 200 OK
```

### 4.4 终端 2：Webbench 命令

```bash
# 若已按方式 A 把 test_pressure 放在项目根目录（推荐）：
./test_pressure/webbench-1.5/webbench -c 100  -t 5 http://127.0.0.1:8080/index.html
./test_pressure/webbench-1.5/webbench -c 500  -t 5 http://127.0.0.1:8080/index.html
./test_pressure/webbench-1.5/webbench -c 1000 -t 5 http://127.0.0.1:8080/index.html

# 若 webbench 在其它路径，可设变量：
# WEBBENCH=/path/to/webbench
# $WEBBENCH -c 100 -t 5 http://127.0.0.1:8080/index.html
```

| 参数 | 含义 |
|------|------|
| `-c` | 并发客户端数（webbench 用多进程模拟） |
| `-t` | 持续秒数 |
| URL | **建议带具体文件** `/index.html`，避免部分环境下目录 `/` 行为不一致 |

### 4.5 压测时注意

1. **一次只改一个变量**（例如只改 `-t` 线程数，日志始终关）。
2. **server 重启**后再测下一组（改 `-t` 必须重启 server；改 webbench 的 `-c` 不用）。
3. 观察 server 终端是否刷 `ERROR`、`accept` 失败；观察 `logs/server.log` 是否在 `-c 1` 时几乎不增长。
4. 压测结束用 `Ctrl+C` 停 server，检查无僵尸进程：`ps aux | grep server`。

---

## 第五部分：step11 代码增量改动

### 5.1 怎么做 step11.cpp

```bash
cp Step1to6/step10.cpp Step1to6/step11.cpp
# 在 step11.cpp 里按下面「替换 / 新增」修改
```

其余 HTTP、MySQL、日志、epoll 逻辑 **与 step10 完全相同**，不要动业务。

### 5.2 用配置结构体替代写死的常量

**删除** step10 里这些「压测相关」常量：

```cpp
const bool LOG_ASYNC = true;
const int PORT = 8080;
const size_t THREAD_POOL_SIZE = 4;
const int CONN_TIMEOUT_SEC = 15;
```

**改为**（放在 `using namespace std;` 之后）：

```cpp
struct ServerConfig {
    string web_root = "www";
    string log_path = "logs/server.log";
    int port = 8080;
    size_t thread_pool_size = 4;
    int conn_timeout_sec = 15;
    bool log_async = true;
    bool close_log = false;      // true = 不写任何日志（压测用）
    bool enable_timer = true;    // false = 关闭空闲踢连接（对比实验用）
    size_t max_body_size = 1024 * 1024;
    int max_events = 64;
    int listen_backlog = 1024;   // step10 是 128，压测可略增
};

ServerConfig g_cfg;

void print_usage(const char* prog) {
    cerr << "用法: " << prog << " [选项]\n"
         << "  -p <port>       监听端口，默认 8080\n"
         << "  -t <threads>    线程池大小，默认 4\n"
         << "  -c <0|1>        是否关闭日志：0=开 1=关，默认 0\n"
         << "  --no-timer      关闭空闲连接定时器\n"
         << "  -h              帮助\n"
         << "\n压测示例: " << prog << " -p 8080 -t 8 -c 1\n";
}

bool parse_config(int argc, char* argv[]) {
    for (int i = 1; i < argc; ++i) {
        string arg = argv[i];
        if (arg == "-h" || arg == "--help") {
            print_usage(argv[0]);
            return false;
        }
        if (arg == "-p" && i + 1 < argc) {
            g_cfg.port = stoi(argv[++i]);
        } else if (arg == "-t" && i + 1 < argc) {
            g_cfg.thread_pool_size = static_cast<size_t>(stoi(argv[++i]));
        } else if (arg == "-c" && i + 1 < argc) {
            g_cfg.close_log = (stoi(argv[++i]) != 0);
        } else if (arg == "--no-timer") {
            g_cfg.enable_timer = false;
        } else {
            cerr << "未知参数: " << arg << "\n";
            print_usage(argv[0]);
            return false;
        }
    }
    if (g_cfg.thread_pool_size == 0) g_cfg.thread_pool_size = 1;
    return true;
}
```

MySQL 相关常量（`MYSQL_HOST` 等）**保持 step10 不变**。

### 5.3 日志宏：支持「关日志」

把原来的：

```cpp
#define LOG_INFO(msg)  Log::instance().write(LogLevel::Info,  msg)
```

**改成**：

```cpp
#define LOG_INFO(msg)  do { if (!g_cfg.close_log) Log::instance().write(LogLevel::Info,  msg); } while(0)
#define LOG_WARN(msg)  do { if (!g_cfg.close_log) Log::instance().write(LogLevel::Warn,  msg); } while(0)
#define LOG_ERROR(msg) do { if (!g_cfg.close_log) Log::instance().write(LogLevel::Error, msg); } while(0)
```

> `LOG_ERROR` 也可在关日志时仍打印——学习阶段统一关即可；排错时再开日志。

### 5.4 修改 `main` 入口

```cpp
int main(int argc, char* argv[]) {
    if (!parse_config(argc, argv)) return 0;

    if (!g_cfg.close_log)
        Log::instance().init(g_cfg.log_path, g_cfg.log_async);

    try {
        SqlPool::instance().init(MYSQL_HOST, MYSQL_PORT, MYSQL_USER,
                                 MYSQL_PASS, MYSQL_DB, MYSQL_POOL_SIZE);
    } catch (const exception& e) {
        cerr << "MySQL 初始化失败: " << e.what() << "\n";
        return 1;
    }

    ThreadPool pool(g_cfg.thread_pool_size);
    unordered_map<int, HttpConnection> connections;

    int listen_fd = socket(AF_INET, SOCK_STREAM, 0);
    if (listen_fd < 0) return 1;
    int opt = 1;
    setsockopt(listen_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
    sockaddr_in addr{};
    addr.sin_family = AF_INET;
    addr.sin_addr.s_addr = INADDR_ANY;
    addr.sin_port = htons(g_cfg.port);
    bind(listen_fd, (sockaddr*)&addr, sizeof(addr));
    listen(listen_fd, g_cfg.listen_backlog);
    setNonBlocking(listen_fd);

    // … epoll、eventfd、主循环与 step10 相同 …

    cout << "Step11 已启动 http://127.0.0.1:" << g_cfg.port << "\n";
    cout << "线程=" << g_cfg.thread_pool_size
         << " 日志=" << (g_cfg.close_log ? "关" : "开")
         << " 定时器=" << (g_cfg.enable_timer ? "开" : "关") << "\n";
    if (!g_cfg.close_log)
        LOG_INFO("Step11 服务器启动");

    // 主循环末尾，原 tick_expired_connections 改为：
    if (g_cfg.enable_timer)
        tick_expired_connections(epfd, connections);

    return 0;
}
```

把 step10 里所有 `PORT` → `g_cfg.port`，`THREAD_POOL_SIZE` → `g_cfg.thread_pool_size`，`CONN_TIMEOUT_SEC` → `g_cfg.conn_timeout_sec`，`WEB_ROOT` → `g_cfg.web_root`（若 `pathToFile` 等处用了 `WEB_ROOT`）。

### 5.5 可选：减少热路径日志

`handle_read` 里若有：

```cpp
LOG_INFO("请求完整 " + req.method + " " + req.path);
```

压测静态页时这条会 **每条请求打一次**，即使用异步日志也有开销。Step11 可改为：

```cpp
if (!g_cfg.close_log)
    LOG_INFO("请求完整 " + req.method + " " + req.path);
```

或干脆删掉，只保留 `ERROR` 路径日志。

### 5.6 增量改动清单

| 位置 | 改动 |
|------|------|
| 全局 | `ServerConfig g_cfg` + `parse_config` |
| `LOG_*` 宏 | 尊重 `close_log` |
| `main` | `argc/argv`，`listen_backlog`，条件 `Log::init` |
| 主循环 | `enable_timer` 控制 `tick_expired_connections` |
| 其它 | **与 step10 相同** |

### 5.7 完整代码

> 保存为 **`Step1to6/step11.cpp`**（或在项目根目录 `step11.cpp`）。在 step10 基础上增加命令行配置；业务逻辑与 step10 相同。修改 `DB_PASS` 为你本机 MySQL 密码。

```cpp
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <vector>
#include <queue>
#include <map>
#include <unordered_map>
#include <thread>
#include <mutex>
#include <condition_variable>
#include <functional>
#include <algorithm>
#include <cctype>
#include <ctime>
#include <unistd.h>
#include <fcntl.h>
#include <cerrno>
#include <sys/epoll.h>
#include <sys/eventfd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <mysql/mysql.h>

using namespace std;

struct ServerConfig {
    string web_root = "www";
    string log_path = "logs/server.log";
    int port = 8080;
    size_t thread_pool_size = 4;
    int conn_timeout_sec = 15;
    bool log_async = true;
    bool close_log = false;
    bool enable_timer = true;
    size_t max_body_size = 1024 * 1024;
    int max_events = 64;
    int listen_backlog = 1024;
};

ServerConfig g_cfg;

void print_usage(const char* prog) {
    cerr << "用法: " << prog << " [选项]\n"
         << "  -p <port>       监听端口，默认 8080\n"
         << "  -t <threads>    线程池大小，默认 4\n"
         << "  -c <0|1>        是否关闭日志：0=开 1=关，默认 0\n"
         << "  --no-timer      关闭空闲连接定时器\n"
         << "  -h              帮助\n"
         << "\n压测示例: " << prog << " -p 8080 -t 8 -c 1\n";
}

bool parse_config(int argc, char* argv[]) {
    for (int i = 1; i < argc; ++i) {
        string arg = argv[i];
        if (arg == "-h" || arg == "--help") {
            print_usage(argv[0]);
            return false;
        }
        if (arg == "-p" && i + 1 < argc) {
            g_cfg.port = stoi(argv[++i]);
        } else if (arg == "-t" && i + 1 < argc) {
            g_cfg.thread_pool_size = static_cast<size_t>(stoi(argv[++i]));
        } else if (arg == "-c" && i + 1 < argc) {
            g_cfg.close_log = (stoi(argv[++i]) != 0);
        } else if (arg == "--no-timer") {
            g_cfg.enable_timer = false;
        } else {
            cerr << "未知参数: " << arg << "\n";
            print_usage(argv[0]);
            return false;
        }
    }
    if (g_cfg.thread_pool_size == 0) g_cfg.thread_pool_size = 1;
    return true;
}

// ========== 数据库配置（本机修改，勿提交 Git）==========
const char* DB_HOST = "127.0.0.1";
const int   DB_PORT = 3306;
const char* DB_USER = "root";
const char* DB_PASS = "your_password";
const char* DB_NAME = "webserver";
const size_t DB_POOL_SIZE = 8;

enum class LogLevel { Debug, Info, Warn, Error };
enum class ParseState { RequestLine, Header, Content };
enum class ReadResult { Incomplete, Complete, Error };
enum class WriteResult { Incomplete, Complete, Error };
enum class ConnPhase { Reading, WaitingResponse, Writing };

class Log {
public:
    static Log& instance() { static Log inst; return inst; }
    void init(const string& path, bool async) {
        async_ = async;
        file_.open(path, ios::app);
        if (!file_) { cerr << "无法打开日志: " << path << "\n"; return; }
        if (async_) { stop_ = false; worker_ = thread([this]() { log_thread_loop(); }); }
    }
    void write(LogLevel level, const string& msg) {
        string line = format_line(level, msg);
        if (async_) write_async(line); else write_sync(line);
    }
    ~Log() {
        if (async_ && worker_.joinable()) {
            { lock_guard<mutex> lock(mtx_); stop_ = true; }
            cv_.notify_all(); worker_.join();
        }
        if (file_.is_open()) file_.close();
    }
private:
    Log() = default;
    string format_line(LogLevel level, const string& msg) {
        time_t now = time(nullptr);
        char tbuf[32];
        strftime(tbuf, sizeof(tbuf), "%Y-%m-%d %H:%M:%S", localtime(&now));
        const char* tag = "INFO";
        switch (level) {
        case LogLevel::Debug: tag = "DEBUG"; break;
        case LogLevel::Info:  tag = "INFO";  break;
        case LogLevel::Warn:  tag = "WARN";  break;
        case LogLevel::Error: tag = "ERROR"; break;
        }
        return string(tbuf) + " [" + tag + "] " + msg;
    }
    void write_sync(const string& line) {
        lock_guard<mutex> lock(mtx_);
        if (file_.is_open()) { file_ << line << '\n'; file_.flush(); }
    }
    void write_async(const string& line) {
        { lock_guard<mutex> lock(mtx_); queue_.push(line); }
        cv_.notify_one();
    }
    void log_thread_loop() {
        while (true) {
            string line;
            { unique_lock<mutex> lock(mtx_);
              cv_.wait(lock, [this]() { return stop_ || !queue_.empty(); });
              if (stop_ && queue_.empty()) return;
              line = move(queue_.front()); queue_.pop(); }
            if (file_.is_open()) { file_ << line << '\n'; file_.flush(); }
        }
    }
    ofstream file_;
    bool async_ = true, stop_ = false;
    queue<string> queue_;
    mutex mtx_;
    condition_variable cv_;
    thread worker_;
};
#define LOG_INFO(msg)  do { if (!g_cfg.close_log) Log::instance().write(LogLevel::Info,  msg); } while(0)
#define LOG_WARN(msg)  do { if (!g_cfg.close_log) Log::instance().write(LogLevel::Warn,  msg); } while(0)
#define LOG_ERROR(msg) do { if (!g_cfg.close_log) Log::instance().write(LogLevel::Error, msg); } while(0)

class SqlPool {
public:
    static SqlPool& instance() { static SqlPool p; return p; }
    void init(const char* host, int port, const char* user,
              const char* pass, const char* db, size_t poolSize) {
        for (size_t i = 0; i < poolSize; ++i) {
            MYSQL* conn = create_connection(host, port, user, pass, db);
            if (!conn) throw runtime_error("MySQL 连接池初始化失败");
            pool_.push(conn);
        }
        LOG_INFO("MySQL 连接池就绪 size=" + to_string(poolSize));
    }
    MYSQL* get() {
        unique_lock<mutex> lock(mtx_);
        cv_.wait(lock, [this]() { return !pool_.empty(); });
        MYSQL* conn = pool_.front();
        pool_.pop();
        return conn;
    }
    void release(MYSQL* conn) {
        if (!conn) return;
        lock_guard<mutex> lock(mtx_);
        pool_.push(conn);
        cv_.notify_one();
    }
private:
    SqlPool() = default;
    MYSQL* create_connection(const char* host, int port,
                             const char* user, const char* pass, const char* db) {
        MYSQL* conn = mysql_init(nullptr);
        if (!conn) return nullptr;
        if (!mysql_real_connect(conn, host, user, pass, db, port, nullptr, 0)) {
            LOG_ERROR(string("mysql_real_connect: ") + mysql_error(conn));
            mysql_close(conn);
            return nullptr;
        }
        mysql_set_character_set(conn, "utf8mb4");
        return conn;
    }
    queue<MYSQL*> pool_;
    mutex mtx_;
    condition_variable cv_;
};

struct ConnectionRAII {
    MYSQL* conn;
    ConnectionRAII() : conn(SqlPool::instance().get()) {}
    ~ConnectionRAII() { SqlPool::instance().release(conn); }
};

struct HttpRequest {
    string method, path, version, body;
    map<string, string> headers;
    size_t content_length = 0;
};

class HttpConnection {
public:
    explicit HttpConnection(int fd) : fd_(fd), last_active_(time(nullptr)) {}
    int fd_;
    ParseState state_ = ParseState::RequestLine;
    string read_buf_;
    HttpRequest request_;
    time_t last_active_;
    ConnPhase phase_ = ConnPhase::Reading;
    string write_buf_;
    size_t write_idx_ = 0;
    void refresh_active() { last_active_ = time(nullptr); }
    bool is_expired(int t) const { return difftime(time(nullptr), last_active_) >= t; }
};

struct ReadyResponse { int fd; string data; };
queue<ReadyResponse> g_ready_queue;
mutex g_ready_mtx;
int g_notify_fd = -1;

void wake_main_thread() {
    if (g_notify_fd < 0) return;
    uint64_t one = 1;
    write(g_notify_fd, &one, sizeof(one));
}

static string trim(const string& s) {
    size_t a = s.find_first_not_of(" \t");
    if (a == string::npos) return "";
    size_t b = s.find_last_not_of(" \t");
    return s.substr(a, b - a + 1);
}
static string toLower(string s) {
    for (char& c : s) c = static_cast<char>(tolower(static_cast<unsigned char>(c)));
    return s;
}

bool get_line(string& buf, string& line) {
    size_t p = buf.find("\r\n");
    if (p == string::npos) return false;
    line = buf.substr(0, p); buf.erase(0, p + 2);
    return true;
}
bool parse_request_line(const string& line, HttpRequest& req) {
    istringstream iss(line);
    return static_cast<bool>(iss >> req.method >> req.path >> req.version);
}
void parse_header_line(const string& line, HttpRequest& req) {
    size_t c = line.find(':');
    if (c == string::npos) return;
    string k = toLower(trim(line.substr(0, c)));
    string v = trim(line.substr(c + 1));
    req.headers[k] = v;
    if (k == "content-length") req.content_length = static_cast<size_t>(stoul(v));
}

ReadResult process_read(HttpConnection& conn) {
    string line;
    while (true) {
        if (conn.state_ == ParseState::Content) {
            if (conn.request_.content_length > g_cfg.max_body_size) return ReadResult::Error;
            if (conn.read_buf_.size() >= conn.request_.content_length) {
                conn.request_.body = conn.read_buf_.substr(0, conn.request_.content_length);
                conn.read_buf_.erase(0, conn.request_.content_length);
                return ReadResult::Complete;
            }
            return ReadResult::Incomplete;
        }
        if (!get_line(conn.read_buf_, line)) return ReadResult::Incomplete;
        switch (conn.state_) {
        case ParseState::RequestLine:
            if (!parse_request_line(line, conn.request_)) return ReadResult::Error;
            conn.state_ = ParseState::Header; break;
        case ParseState::Header:
            if (line.empty()) {
                if (conn.request_.method == "POST" && conn.request_.content_length > 0)
                    conn.state_ = ParseState::Content;
                else return ReadResult::Complete;
            } else parse_header_line(line, conn.request_);
            break;
        case ParseState::Content: break;
        }
    }
}

bool append_read(HttpConnection& conn) {
    char buf[8192];
    ssize_t n = read(conn.fd_, buf, sizeof(buf));
    if (n < 0) return (errno == EAGAIN || errno == EWOULDBLOCK);
    if (n == 0) return false;
    conn.read_buf_.append(buf, static_cast<size_t>(n));
    return true;
}

string buildHttpResponse(int code, const string& text, const string& type, const string& body) {
    return "HTTP/1.1 " + to_string(code) + " " + text + "\r\n"
           "Content-Type: " + type + "\r\n"
           "Content-Length: " + to_string(body.size()) + "\r\n"
           "Connection: close\r\n\r\n" + body;
}

string buildRedirectResponse(const string& location) {
    return "HTTP/1.1 302 Found\r\nLocation: " + location + "\r\n"
           "Content-Length: 0\r\nConnection: close\r\n\r\n";
}

string pathToFile(const string& urlPath) {
    if (urlPath == "/" || urlPath == "/index.html") return g_cfg.web_root + "/index.html";
    if (urlPath == "/echo" || urlPath == "/echo.html") return g_cfg.web_root + "/echo.html";
    if (urlPath == "/log" || urlPath == "/log.html") return g_cfg.web_root + "/log.html";
    if (urlPath == "/register" || urlPath == "/register.html") return g_cfg.web_root + "/register.html";
    if (urlPath == "/welcome" || urlPath == "/welcome.html") return g_cfg.web_root + "/welcome.html";
    if (urlPath == "/login_error.html") return g_cfg.web_root + "/login_error.html";
    if (urlPath == "/register_error.html") return g_cfg.web_root + "/register_error.html";
    return g_cfg.web_root + urlPath;
}

bool isPathSafe(const string& p) { return p.find("..") == string::npos; }

string getContentType(const string& fp) {
    if (fp.size() >= 5 && fp.substr(fp.size()-5)==".html") return "text/html; charset=utf-8";
    if (fp.size() >= 4 && fp.substr(fp.size()-4)==".css") return "text/css; charset=utf-8";
    return "application/octet-stream";
}

bool readFile(const string& fp, string& out) {
    ifstream ifs(fp, ios::binary);
    if (!ifs) return false;
    out.assign((istreambuf_iterator<char>(ifs)), istreambuf_iterator<char>());
    return true;
}

string url_decode(const string& s) {
    string out;
    for (size_t i = 0; i < s.size(); ++i) {
        if (s[i] == '+') out += ' ';
        else if (s[i] == '%' && i + 2 < s.size()) {
            int hi = s[i+1], lo = s[i+2];
            auto hex = [](int c) { return (c>='0'&&c<='9')?c-'0':(c>='A'&&c<='F')?c-'A'+10:(c>='a'&&c<='f')?c-'a'+10:-1; };
            int h = hex(hi), l = hex(lo);
            if (h >= 0 && l >= 0) { out += static_cast<char>((h<<4)|l); i += 2; }
            else out += s[i];
        } else out += s[i];
    }
    return out;
}

map<string, string> parse_form_urlencoded(const string& body) {
    map<string, string> m;
    size_t start = 0;
    while (start < body.size()) {
        size_t amp = body.find('&', start);
        string pair = body.substr(start, amp == string::npos ? string::npos : amp - start);
        size_t eq = pair.find('=');
        if (eq != string::npos)
            m[url_decode(pair.substr(0, eq))] = url_decode(pair.substr(eq + 1));
        if (amp == string::npos) break;
        start = amp + 1;
    }
    return m;
}

string escape_sql(const string& s) {
    string out;
    for (char c : s) {
        if (c == '\'') out += "''";
        else if (c == '\\') out += "\\\\";
        else out += c;
    }
    return out;
}

string buildGetResponse(const HttpRequest& req) {
    if (!isPathSafe(req.path))
        return buildHttpResponse(403, "Forbidden", "text/plain; charset=utf-8", "403");
    string fp = pathToFile(req.path), body;
    if (!readFile(fp, body))
        return buildHttpResponse(404, "Not Found", "text/plain; charset=utf-8", "404: " + req.path);
    return buildHttpResponse(200, "OK", getContentType(fp), body);
}

string handle_register(const HttpRequest& req) {
    auto form = parse_form_urlencoded(req.body);
    string user = form["username"];
    string pass = form["passwd"];
    if (user.empty() || pass.empty())
        return buildRedirectResponse("/register_error.html");
    ConnectionRAII raii;
    MYSQL* conn = raii.conn;
    string sql = "INSERT INTO user(username, password) VALUES('"
               + escape_sql(user) + "','" + escape_sql(pass) + "')";
    if (mysql_query(conn, sql.c_str()) != 0) {
        LOG_WARN(string("注册失败: ") + mysql_error(conn));
        return buildRedirectResponse("/register_error.html");
    }
    LOG_INFO("注册成功 user=" + user);
    return buildRedirectResponse("/welcome.html");
}

string handle_login(const HttpRequest& req) {
    auto form = parse_form_urlencoded(req.body);
    string user = form["username"];
    string pass = form["passwd"];
    if (user.empty() || pass.empty())
        return buildRedirectResponse("/login_error.html");
    ConnectionRAII raii;
    MYSQL* conn = raii.conn;
    string sql = "SELECT password FROM user WHERE username='"
               + escape_sql(user) + "' LIMIT 1";
    if (mysql_query(conn, sql.c_str()) != 0) {
        LOG_ERROR(string("登录查询失败: ") + mysql_error(conn));
        return buildRedirectResponse("/login_error.html");
    }
    MYSQL_RES* res = mysql_store_result(conn);
    if (!res) return buildRedirectResponse("/login_error.html");
    MYSQL_ROW row = mysql_fetch_row(res);
    bool ok = row && row[0] && string(row[0]) == pass;
    mysql_free_result(res);
    if (ok) {
        LOG_INFO("登录成功 user=" + user);
        return buildRedirectResponse("/welcome.html");
    }
    LOG_WARN("登录失败 user=" + user);
    return buildRedirectResponse("/login_error.html");
}

string buildPostEchoResponse(const HttpRequest& req) {
    return buildHttpResponse(200, "OK", "text/plain; charset=utf-8",
        "收到 POST body:\n" + req.body);
}

string do_request(const HttpRequest& req) {
    if (req.method == "GET") return buildGetResponse(req);
    if (req.method == "POST") {
        if (req.path == "/echo") return buildPostEchoResponse(req);
        if (req.path == "/register") return handle_register(req);
        if (req.path == "/login") return handle_login(req);
        return buildHttpResponse(404, "Not Found", "text/plain; charset=utf-8",
            "未知 POST: " + req.path);
    }
    return buildHttpResponse(405, "Method Not Allowed", "text/plain; charset=utf-8", "405");
}

class ThreadPool {
public:
    explicit ThreadPool(size_t n) : stop_(false) {
        for (size_t i = 0; i < n; ++i) workers_.emplace_back([this]() { workerLoop(); });
    }
    ~ThreadPool() {
        { lock_guard<mutex> lk(mtx_); stop_ = true; }
        cv_.notify_all();
        for (auto& t : workers_) if (t.joinable()) t.join();
    }
    void submit(function<void()> task) {
        { lock_guard<mutex> lk(mtx_); tasks_.push(move(task)); }
        cv_.notify_one();
    }
private:
    void workerLoop() {
        while (true) {
            function<void()> task;
            { unique_lock<mutex> lk(mtx_);
              cv_.wait(lk, [this]() { return stop_ || !tasks_.empty(); });
              if (stop_ && tasks_.empty()) return;
              task = move(tasks_.front()); tasks_.pop(); }
            task();
        }
    }
    queue<function<void()>> tasks_;
    mutex mtx_; condition_variable cv_;
    vector<thread> workers_; bool stop_;
};

void setNonBlocking(int fd) {
    fcntl(fd, F_SETFL, fcntl(fd, F_GETFL, 0) | O_NONBLOCK);
}
void addFd(int epfd, int fd, uint32_t ev = EPOLLIN) {
    epoll_event e{}; e.events = ev; e.data.fd = fd;
    epoll_ctl(epfd, EPOLL_CTL_ADD, fd, &e);
}
void modFd(int epfd, int fd, uint32_t ev) {
    epoll_event e{}; e.events = ev; e.data.fd = fd;
    epoll_ctl(epfd, EPOLL_CTL_MOD, fd, &e);
}
void removeFd(int epfd, int fd) { epoll_ctl(epfd, EPOLL_CTL_DEL, fd, nullptr); }

void closeConnection(int epfd, int fd, unordered_map<int, HttpConnection>& conns) {
    auto it = conns.find(fd);
    if (it == conns.end()) return;
    removeFd(epfd, fd); close(fd); conns.erase(it);
}

void tick_expired_connections(int epfd, unordered_map<int, HttpConnection>& conns) {
    vector<int> v;
    for (auto& [fd, c] : conns)
        if (c.phase_ == ConnPhase::Reading && c.is_expired(g_cfg.conn_timeout_sec)) v.push_back(fd);
    for (int fd : v) { LOG_WARN("超时 fd=" + to_string(fd)); closeConnection(epfd, fd, conns); }
}

WriteResult process_write(int epfd, HttpConnection& c) {
    c.refresh_active();
    while (c.write_idx_ < c.write_buf_.size()) {
        ssize_t n = write(c.fd_, c.write_buf_.data() + c.write_idx_,
                          c.write_buf_.size() - c.write_idx_);
        if (n > 0) { c.write_idx_ += static_cast<size_t>(n); continue; }
        if (n < 0 && (errno == EAGAIN || errno == EWOULDBLOCK)) {
            modFd(epfd, c.fd_, EPOLLOUT); return WriteResult::Incomplete;
        }
        return WriteResult::Error;
    }
    return WriteResult::Complete;
}

void handle_write(int epfd, HttpConnection& c, unordered_map<int, HttpConnection>& conns) {
    if (c.phase_ != ConnPhase::Writing) return;
    WriteResult w = process_write(epfd, c);
    if (w == WriteResult::Complete || w == WriteResult::Error)
        closeConnection(epfd, c.fd_, conns);
}

void drain_ready_responses(int epfd, unordered_map<int, HttpConnection>& conns) {
    queue<ReadyResponse> local;
    { lock_guard<mutex> lk(g_ready_mtx); swap(local, g_ready_queue); }
    while (!local.empty()) {
        auto rr = move(local.front()); local.pop();
        auto it = conns.find(rr.fd);
        if (it == conns.end()) continue;
        HttpConnection& c = it->second;
        if (c.phase_ != ConnPhase::WaitingResponse) continue;
        c.write_buf_ = move(rr.data); c.write_idx_ = 0; c.phase_ = ConnPhase::Writing;
        modFd(epfd, c.fd_, EPOLLOUT);
        WriteResult w = process_write(epfd, c);
        if (w == WriteResult::Complete || w == WriteResult::Error)
            closeConnection(epfd, c.fd_, conns);
    }
}

void handle_read(int epfd, HttpConnection& c, ThreadPool& pool,
                 unordered_map<int, HttpConnection>& conns) {
    if (c.phase_ != ConnPhase::Reading) return;
    if (!append_read(c)) { closeConnection(epfd, c.fd_, conns); return; }
    c.refresh_active();
    ReadResult r = process_read(c);
    if (r == ReadResult::Incomplete) return;
    if (r == ReadResult::Error) { closeConnection(epfd, c.fd_, conns); return; }
    HttpRequest req = move(c.request_);
    int fd = c.fd_;
    c.phase_ = ConnPhase::WaitingResponse;
    modFd(epfd, fd, 0);
    if (!g_cfg.close_log)
        LOG_INFO("请求完整 " + req.method + " " + req.path);
    pool.submit([fd, req]() {
        string resp = do_request(req);
        { lock_guard<mutex> lk(g_ready_mtx); g_ready_queue.push({fd, move(resp)}); }
        wake_main_thread();
    });
}

int main(int argc, char* argv[]) {
    if (!parse_config(argc, argv)) return 0;

    if (!g_cfg.close_log)
        Log::instance().init(g_cfg.log_path, g_cfg.log_async);

    try {
        SqlPool::instance().init(DB_HOST, DB_PORT, DB_USER,
                                 DB_PASS, DB_NAME, DB_POOL_SIZE);
    } catch (const exception& e) {
        cerr << "MySQL 初始化失败: " << e.what() << "\n";
        return 1;
    }

    ThreadPool pool(g_cfg.thread_pool_size);
    unordered_map<int, HttpConnection> connections;

    int listen_fd = socket(AF_INET, SOCK_STREAM, 0);
    if (listen_fd < 0) return 1;
    int opt = 1;
    setsockopt(listen_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
    sockaddr_in addr{};
    addr.sin_family = AF_INET;
    addr.sin_addr.s_addr = INADDR_ANY;
    addr.sin_port = htons(g_cfg.port);
    bind(listen_fd, (sockaddr*)&addr, sizeof(addr));
    listen(listen_fd, g_cfg.listen_backlog);
    setNonBlocking(listen_fd);

    int epfd = epoll_create1(0);
    addFd(epfd, listen_fd);
    g_notify_fd = eventfd(0, EFD_NONBLOCK);
    addFd(epfd, g_notify_fd);

    cout << "Step11 已启动 http://127.0.0.1:" << g_cfg.port << "\n";
    cout << "线程=" << g_cfg.thread_pool_size
         << " 日志=" << (g_cfg.close_log ? "关" : "开")
         << " 定时器=" << (g_cfg.enable_timer ? "开" : "关") << "\n";
    if (!g_cfg.close_log)
        LOG_INFO("Step11 服务器启动");

    vector<epoll_event> events(g_cfg.max_events);
    while (true) {
        int n = epoll_wait(epfd, events.data(), g_cfg.max_events, -1);
        if (n < 0) continue;
        for (int i = 0; i < n; ++i) {
            int fd = events[i].data.fd;
            uint32_t ev = events[i].events;
            if (fd == g_notify_fd) {
                uint64_t u; while (read(g_notify_fd, &u, sizeof(u)) > 0) {}
                drain_ready_responses(epfd, connections); continue;
            }
            if (fd == listen_fd) {
                while (true) {
                    sockaddr_in ca{}; socklen_t len = sizeof(ca);
                    int cfd = accept(listen_fd, (sockaddr*)&ca, &len);
                    if (cfd < 0) { if (errno==EAGAIN||errno==EWOULDBLOCK) break; break; }
                    setNonBlocking(cfd);
                    addFd(epfd, cfd);
                    connections.emplace(cfd, HttpConnection(cfd));
                }
            } else {
                auto it = connections.find(fd);
                if (it == connections.end()) continue;
                if (ev & EPOLLIN) handle_read(epfd, it->second, pool, connections);
                if (ev & EPOLLOUT) {
                    it = connections.find(fd);
                    if (it != connections.end()) handle_write(epfd, it->second, connections);
                }
            }
        }
        drain_ready_responses(epfd, connections);
        if (g_cfg.enable_timer)
            tick_expired_connections(epfd, connections);
    }
    return 0;
}
```

---

## 第六部分：对比实验怎么设计

### 6.1 实验记录表（复制到笔记）

固定：URL = `http://127.0.0.1:8080/index.html`，webbench `-t 5`，关 MySQL 业务（只测静态）。

| 编号 | server 命令 | webbench `-c` | QPS | 失败/超时 | 备注 |
|------|-------------|---------------|-----|-----------|------|
| 1 | `./server -t 2 -c 1` | 500 | | | 基线 |
| 2 | `./server -t 4 -c 1` | 500 | `234288 ÷ 60 ≈ 3904.8` 次/秒 | 0 | 线程 ↑ |
| 3 | `./server -t 8 -c 1` | 500 | | | 线程 ↑ |
| 4 | `./server -t 4 -c 0` | 500 | | | 开日志对比 |
| 5 | `./server -t 4 -c 1 --no-timer` | 500 | | | 关定时器 |
| 6 | `./server -t 4 -c 1` | 1000 | | | 并发 ↑ |

### 6.2 预期趋势（多数环境）

| 对比 | 预期 |
|------|------|
| `-c 1` vs `-c 0`（关 vs 开日志） | 关日志 **QPS 更高** |
| `-t 2` → `4` → `8` | 先升后平或略降（CPU 核数、锁竞争） |
| `--no-timer` | 静态短连接压测，影响通常 **很小** |
| `-c 1000` 且 `ulimit` 太低 | 失败数上升、QPS 暴跌 |

### 6.3 Step12 之后再做的实验

完成 [Step12（可选）](./Step5之后完善路线图.md#九step12可选etepolloneshotkeep-alive) 后，可追加：

- LT vs ET
- Keep-Alive on/off（同一连接多次 GET）

---

## 第七部分：如何读懂 Webbench 输出

示例输出（数值为虚构）：

```text
Benchmarking 127.0.0.1 (be patient)
......

Server Software:        MyWebServer
Server Hostname:        127.0.0.1
Server Port:            8080

Document Path:          /index.html
Document Length:        1234 bytes

Concurrency Level:      500
Time taken for tests:   5.003 seconds
Complete requests:      62000
Failed requests:        0
Total transferred:      78900000 bytes
HTML transferred:       76543200 bytes
Requests per second:    12395.23 [#/sec] (mean)
Time per request:       40.335 [ms] (mean)
Time per request:       0.081 [ms] (mean, across all concurrent requests)
Transfer rate:          15400.00 [Kbytes/sec] received

Connection Times (ms)
              min  mean[+/-sd] median   max
Connect:        0    1   0.5      1       5
Processing:     5   35  10.0     32     120
Waiting:        4   34   9.8     31     118
Total:          6   36  10.2     33     125
```

| 字段 | 看什么 |
|------|--------|
| **Requests per second** | 核心 **QPS** |
| **Failed requests** | 应为 **0**；非 0 先查 fd、server 崩溃、端口 |
| **Complete requests** | 总完成数 ≈ QPS × 时间 |
| **Time per request (mean)** | 并发越高通常越大 |
| **Document Length** | 应为 `index.html` 实际大小；0 或极小可能 404 |

---

## 编译、运行与测试

### 编译

```bash
# 项目根目录
g++ -std=c++17 -Wall -pthread -o server Step1to6/step11.cpp -lmysqlclient
```

### 功能回归（压测前必做）

```bash
./server -p 8080 -t 4 -c 0
curl http://127.0.0.1:8080/
curl -X POST http://127.0.0.1:8080/echo -d "a=b"
# 浏览器：register.html / log.html 仍能登录
```

### 压测验收标准

- [ ] webbench `-c 100~1000`、`-t 5`，**Failed requests = 0**（或极少数可解释失败）
- [ ] 记录至少 **3 组** 不同 `-t` 或开/关日志的 QPS，趋势合理
- [ ] 压测后 server 可 `Ctrl+C` 正常退出，无 fd 泄漏式疯狂刷错
- [ ] `-c 1` 时 QPS 明显高于 `-c 0`（同线程同并发）

---

## 常见问题排查

### Q1：`webbench: command not found`

- 使用完整路径：`/path/to/webbench -c 100 ...`
- 或 `export PATH=$PATH:/path/to/webbench/dir`

### Q2：`Failed requests` 很多

| 可能原因 | 处理 |
|----------|------|
| `ulimit -n` 太小 | `ulimit -n 65535` 后重启 server |
| server 未启动或端口错 | `curl -I` 先测 |
| `-c` 过大本机扛不住 | 降低 `-c`，或缩短 `-t` |
| server 打满 CPU 单核 | 看 `top`，适当减 `-c` |

### Q3：QPS 只有几百

- 是否 **开了日志**（`-c 0`）？
- 是否在 **WSL / 虚拟机** 里（性能低于裸机 Linux）？
- 是否误压了会走 MySQL 的路径？
- 线程池是否过小？试 `-t 4` 或 `-t 8`

### Q4：和 Tiny 九万 QPS 差一个数量级

正常。Tiny 宣传值通常包含：**关日志 + LT/ET 调优 + Proactor + 专门环境**。学习项目看 **相对提升** 即可。

### Q5：压测时 MySQL 要关吗？

不必。静态 GET 不走 SQL；只要 `SqlPool::init` 成功即可。若想极致静态压测，可另做「无 MySQL 版」分支——**非本篇必须**。

### Q6：Windows 本机能否压测？

建议在 **WSL2** 里编译 server + webbench，与 Step4～10 一致。

---

## 本篇小结与下一篇预告

### 你现在已经会了什么

- [x] 用 **Webbench** 测 **QPS** 和失败率  
- [x] 压测前 **关日志**、调 **线程数**、检查 **ulimit**  
- [x] **对比实验**记录表，用数据说话  
- [x] **`Step1to6/step11.cpp`** = step10 + 命令行配置  
- [x] 完成 **TinyWebServer 主路线 Step6～11** 全部篇章  

### 路线图

```text
Step10 MySQL + 注册登录
    ▼
Step11 Webbench 压测  ← 本篇（主路线收官）
    ▼
Step12（可选）ET / EPOLLONESHOT / Keep-Alive
    ▼
（可选）拆分多文件、精修对齐 Tiny
```

### 下一篇：Step12（可选）

边沿触发 **EPOLLET**、`EPOLLONESHOT`、**HTTP Keep-Alive** 同一连接处理多个请求。详见路线图 [第九节](./Step5之后完善路线图.md#九step12可选etepolloneshotkeep-alive)。  
若你打算收工写简历，**做到 Step11 已够**；Step12 是性能进阶加分项。

### 易错点速查表

| 错误 | 后果 |
|------|------|
| 压测开着详细请求日志 | QPS 偏低，误以为服务器慢 |
| 不提高 `ulimit` 就大 `-c` | Failed requests 暴增 |
| 用 POST 登录 URL 压测 | 指标无意义 |
| 一次改多个参数 | 不知道谁起作用 |
| 没 `curl` 冒烟就压 | 把「服务没起」当成性能差 |

---

> **恭喜：** 你已经走完 MyWebServer **主路线最后一步**——不仅能写 Web 服务器，还能用 **webbench** 证明它在压力下能稳定干活。接下来可选 Step12 进阶 IO，或按 [分文件编写指南](./分文件编写指南/00-总览与工程结构.md) **拆分 `src/` 多文件工程**。
